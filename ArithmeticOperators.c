#include <stdio.h>

int main(){
   // Arithmetic operators
   // + (addition)
   // - (subtraction)
   // * (multiplication)
   // / (division)
   // % (modulus)
   // ++ increment 
   // -- decrement

   // augmented assignment operators = used to replace a statement where an operator takes a variable as one of its arguments and then assigs the result back to the same variable
    //x = x + 1 => x+=1 
    //x = x - 1 => x-=1
    //x = x * 4 => x*=4
    //x = x / 5 => x/=5
    //x = x % 2 => x%=2

    return 0;
}